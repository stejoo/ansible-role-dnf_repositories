---
galaxy_info:
  standalone: true
  role_name: dnf_repositories
  namespace: stejoo
  author: Stefan Joosten <stefan@atcomputing.nl>
  description: Configure software repositories
  company: AT Computing
  issue_tracker_url: ''
  license: EUPL-1.2
  min_ansible_version: '2.11'

  # Provide a list of supported platforms, and for each platform a list of versions.
  # If you don't wish to enumerate all versions for a particular platform, use 'all'.
  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  #
  platforms:
    - name: Fedora
      versions:
        - all
    - name: EL
      versions:
        - '8'
  # - name: Debian
  #   versions:
  #   - ?

  galaxy_tags:
    #- apt
    #- apt_repositories
    - dnf
    - rpm
    - repository

dependencies: []
